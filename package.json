{
  "name": "@tokenring-ai/app",
  "version": "0.2.0",
  "description": "Base app class for TokenRing applications, which provides a registry of services and an attachment point for plugins.",
  "dependencies": {
    "@tokenring-ai/agent": "0.2.0",
    "@tokenring-ai/utility": "0.2.0",
    "zod": "^4.3.6"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  },
  "types": "./index.ts",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "vitest": "^4.0.18",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  }
}
